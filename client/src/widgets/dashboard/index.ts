// Side-effect imports: registers each dashboard widget into the global
// registry on load. Import this barrel from the dashboard page so the
// registry is populated before getDashboardWidgets() is called.
import "./MyTeam";
import "./TopPerformers";
import "./LeagueTable";
import "./Scoreboard";
import "./PowerRankings";
