/**
 * Algorithm registry bootstrap.
 * Import each algorithm module here — registration is a side effect.
 * Add new algorithms by creating a file in this directory and importing it below.
 */
import "./powerIndex.js";
